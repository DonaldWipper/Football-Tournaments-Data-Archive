CREATE TABLE sports_ru_staging.tournaments
(
    id        bigint,
    name      text,
    tag_id    bigint,
    type      text,
    countries text,
    priority  text,
    flag      text,
    big_logo  text
);